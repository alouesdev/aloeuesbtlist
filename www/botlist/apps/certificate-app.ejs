<%- include('../../parts/head', {bot, user, path}) %>
     <br><br>
<div class="container">
  <br><br>
<div class="container contact-form bg-vote-vcodes">
  <div class="contact-image">
      <i></i>
  </div>
  <form method="post" class="form-discorddark">
      <h3></h3>
     <div class="row">
          <div class="col-md-6">
              <div class="form-group">
                <label><i class="fad fa-database"></i> Botun 50 Sunucunun Üstündemi ?
		<br><small class="text-muted">Başvurununuzun kabul edilmesi için, botunuzun 50 sunucunun üstünde olması gerekir.</small></label>
                <br>
                <select name="onehundred" id="onehundred" required disabled>
                    <option value="Yes" selected>Evet</option>
                    <option value="No">Hayır</option>
                </select>    
              </div>
              <br>
              <div class="form-group">
                  <label>Botunuzu Diğer botlardan ayıran özellikleri kısaca anlatınız.</label>
                  <textarea type="text" id="future" name="future" style="width: 100%;"></textarea>
            </div>
            <br>
              <div class="form-group">
                <label><i class="fad fa-robot"></i> Botunuzu Seçin
                </label>
                <br>
                <select name="bot" id="bot" required>
                  <% userbots.map(a => {%>
                    <% if(a.certificate == "None") {%>
                    <option value="<%= a.botID %>"><%= a.username %></option>
                    <% } %>
                 <% }); %>
              </select>
            </div>
          </div>
      </div>
      <br><br><br><br>
      <button type="submit" name="btnSubmit" class="btn btn-primary"><i class="fad fa-save"></i> Submit</button>
  </form>
</div>
</div>
<br><br>
<script>
    var urlParams = new URLSearchParams(window.location.search);
    if(urlParams.has('success') && urlParams.get('success')){
    Swal.fire({
     icon: 'success',
     title: 'Succesfull...',
     closeOnConfirm: false,
     text: urlParams.get('message'),
     footer: 'Copyright 2021 &copy Gliot Botlist - All right reserved.'
    }).then((result) => {
  if (result.isConfirmed) {
    window.location.href = `/bot/${urlParams.get('botID')}`;
  }
})
    }
    if(urlParams.has('error') && urlParams.get('error')){
    Swal.fire({
     icon: 'error',
     title: 'Oops...',
     text: urlParams.get('message'),
     footer: 'Copyright 2021 &copy Gliot Botlist - All right reserved.'
    }).then((result) => {
  if (result.isConfirmed) {
    window.location.href = '/addbot';
  }    
})
    }
   </script>
<%- include('../../parts/foot') %>
